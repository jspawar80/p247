version: '3.8'

services:
  app-branchname:
    image: username/imagename:branchname
    restart: always
    ports:
      - 3000:3000
    environment:
      -  NODE_ENV=development
      #- NODE_ENV=production
