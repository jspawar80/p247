version: '3.8'

services:
  imagename-branchname:
    image: username/imagename:branchname
    restart: always
    ports:
      - 3001:3000
    environment:
      -  NODE_ENV=development
      #- NODE_ENV=production
